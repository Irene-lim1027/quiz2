<!DOCTYPE html>
<html>
  <head>
    <title>Quiz2</title>
    
    <div class='container'>
    <% flash.each do |type, message| %>
    <div class='shadow-lg p-3 mb-5 bg-<%= type %> rounded'>
    <%= message %>
    </div>
    <% end %>
    </div>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
    <div class='container-fluid'>
        <%= flash[:notice] || flash[:alert] || flash[:danger] %>
        <span class ='navbar-brand mb-0 h1'>💡 Idea Factory 💡
        |
        <%= link_to 'Home', root_path %>
        |
        <%= link_to 'New Idea',new_idea_path %>
        |
        <% if user_signed_in? %>
        Hello,
        <%= current_user.full_name%>
        |
        <%= link_to "Sign Out", session_path, method: :delete, data: {confirm: "Are you sure you want to signed out?"}  %> 
        <% else %>
        <%= link_to "Sign In", new_session_path %> 
        |
        <%= link_to "Sign Up", new_user_path %>    

        <% end %>
      </span>  
  </div>
</nav>

    <%= yield %>
  </body>
</html>
